# OCS 网课助手 · AI 模型配置版 安装说明

生成时间：2026-09-17
版本：4.15.4

---

## 一、在线安装（推荐）

在浏览器地址栏打开：

```
https://ocs-userscript.app.workbuddy.host/ocs-4.15.4-ai-model.user.js
```

ScriptCat（脚本猫）会自动弹出安装页面，点击「安装」即可。

也可手动走 ScriptCat 安装页：

```
chrome-extension://liilgpjgabokdklappibcjfablkpcekh/src/install.html?url=https://ocs-userscript.app.workbuddy.host/ocs-4.15.4-ai-model.user.js
```

---

## 二、手动导入（离线）

1. 打开 ScriptCat 管理页：点击扩展图标 → 「管理脚本」/ 「脚本列表」
   或直接访问：`chrome-extension://liilgpjgabokdklappibcjfablkpcekh/src/options.html`
2. 点击「添加脚本」/ 「导入」按钮
3. 选择本地文件：`ocs-4.15.4-ai-model.user.js`
4. 保存并启用

---

## 三、文件清单

| 文件 | 说明 |
| --- | --- |
| `ocs-4.15.4-ai-model.user.js` | 完整用户脚本（含元数据头 + 内核 + 样式） |
| `ocs.common.user.js` | 同上，构建产物的原始命名 |
| `ocs.meta.js` | 仅元数据，供脚本更新检查使用 |
| `index.js` | vite 打包内核（无元数据头，不可直接导入） |

> ⚠️ 只有 `.user.js` 文件可以直接导入。`index.js` 没有 `==UserScript==` 元数据头，
> 单独导入会失效。

---

## 四、本次升级内容

AI 做题配置从「手填 URL + 模型名」升级为「选择供应商 + 填 API Key」的表单：

- **AI 模型配置** 按钮：打开「添加模型」弹窗
- **供应商下拉**：DeepSeek、腾讯云 Token Plan、Moonshot、通义千问、智谱、
  硅基流动、OpenAI、Ollama、自定义
- **每个供应商带「查看文档」链接**：一键跳转官方文档 / 控制台
- **API Key 密码框**：带 👁 显示/隐藏切换
- **测试连接** 按钮：实时验证 Key 是否可用，返回延迟或中文错误提示
- **模型名**：可手填，也可从供应商预设模型下拉选择
- **当前模型** 只读回显：设置页直接看到正在用的模型
- **旧配置自动迁移**：原先填的 URL 会自动识别并匹配到对应供应商，无需重配

### 支持的供应商与接口

| 供应商 | 接口地址 |
| --- | --- |
| DeepSeek | https://api.deepseek.com/v1 |
| 腾讯云 Token Plan | https://api.lkeap.cloud.tencent.com/v1 |
| Moonshot | https://api.moonshot.cn/v1 |
| 通义千问 | https://dashscope.aliyuncs.com/compatible-mode/v1 |
| 智谱 | https://open.bigmodel.cn/api/paas/v4 |
| 硅基流动 | https://api.siliconflow.cn/v1 |
| OpenAI | https://api.openai.com/v1 |
| Ollama | http://localhost:11434/v1 |

---

## 五、注意

- 脚本 `@connect` 白名单已加入上述全部 AI 域名，跨域请求不会被拦。
- 腾讯云使用 `Authorization: Bearer <key>`；OpenAI 兼容协议同理。
- 若测试连接报 401/403，多为 Key 无效或未开通对应模型权限。
- 报 404 通常是模型名写错或该模型未在账号下开通。
